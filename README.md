# control-plane

Nuxt 4 application deployed on Cloudflare Workers.

Local development can be pinned with `NUXT_PORT`; the app's `nuxt.config.ts`
reads it directly and uses the same port for local `SITE_URL` fallbacks.
